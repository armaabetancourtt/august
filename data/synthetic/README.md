# SYNTHETIC DATA

Everything generated into this directory is simulated.

It exists only to make AUGUST reproducible before public-data adapters are configured.

Synthetic files must never be described as observed market, customer, property or fraud data.
